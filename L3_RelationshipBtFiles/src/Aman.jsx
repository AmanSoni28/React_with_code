
function Ankit(){                              //function name always capital letter
    return  (
     <h1>I Learn React</h1>
    )
}

export default Ankit;

// note: we can return only one html tag 
// for returning more than one tag we use 'Fragment(<>)'